[project]
name = "fly-in"
version = "0.1.0"
description = ""
authors = [
    {name = "haer00reh",email = "haer-reh@student.1337.ma"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pygame (>= 2.6.1)",
    "pydantic (>= 2.13.4)"
]

[tool.poetry]
packages = [{include = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
